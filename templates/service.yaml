apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicdlab-chart.fullname" . }}
  labels:
    {{- include "cicdlab-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cicdlab-chart.selectorLabels" . | nindent 4 }}
