apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cicdlab-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cicdlab-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cicdlab-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
